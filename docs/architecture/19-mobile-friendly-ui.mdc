---
description: 架构文档 - 19 移动端友好 UI 要求
globs: ["**/*"]
alwaysApply: false
---

# 19 移动端友好 UI 要求

本文件描述图形展示与基础界面的移动端适配要求。本文为设计文档，不包含实现代码。

## 适配范围
- lobby / game 两个前端应用
- 地图渲染、基础面板、操作入口

## 设计要求
- 响应式布局，支持常见移动分辨率
- 触控优先，点击热区不小于 44px
- 面板可折叠与分层，避免遮挡主地图
- 关键按钮固定在安全区域内，避免刘海/手势区域
- 地图支持双指缩放与拖拽
- 关键信息优先显示，次要信息收纳到抽屉

## 可用性要求
- 保持 PVP 关键操作在单手可达区域
- 操作流程在移动端不超过 3 层面板深度
- 低端设备允许降级动效，但不影响可操作性
