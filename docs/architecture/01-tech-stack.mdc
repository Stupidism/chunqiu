---
description: 架构文档 - 01 技术栈和架构
globs: ["**/*"]
alwaysApply: false
---

# 01 技术栈和架构

## 技术选型

### 项目管理
| 技术 | 用途 |
|-----|------|
| **Turborepo** | Monorepo 管理，统一构建和依赖 |
| **pnpm** | 包管理器 |

### 前端
| 技术 | 用途 |
|-----|------|
| **Next.js 14+** | React 框架, App Router, Server Actions |
| **Tailwind CSS** | 原子化 CSS 样式系统 |
| **shadcn/ui** | 基础 UI 组件库 |
| **Zustand** | 客户端游戏状态管理 |
| **React Query / SWR** | 服务器状态同步 |
| **TypeScript** | 类型安全 |

### 后端
| 技术 | 用途 |
|-----|------|
| **Next.js API Routes** | REST API 端点 |
| **Server Actions** | 游戏操作 mutations |
| **WebSocket (ws)** | 实时通信 (多人游戏/AI回合通知) |
| **Prisma** | 数据库 ORM |
| **PostgreSQL** | 主数据库，持久化存储 |
| **Speech-to-Text 服务** | 语音输入转文本 (云服务或自建) |
| **语义解析模块** | 将语音文本转为标准 GameAction |

### 基础设施
| 技术 | 用途 |
|-----|------|
| **Vercel** | 前端部署 + Serverless Functions |
| **Vercel Postgres / Neon** | 托管 PostgreSQL |
| **Upstash Redis** | 游戏会话缓存（可选） |

## 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        Turborepo                            │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  apps/                                                  ││
│  │  ├── lobby/       Next.js 大厅                           ││
│  │  ├── game/        Next.js 游戏主界面                     ││
│  │  ├── editor/      Next.js 地图编辑器                     ││
│  │  └── server/      游戏后端服务 (ws + API)                ││
│  ├─────────────────────────────────────────────────────────┤│
│  │  packages/                                              ││
│  │  ├── ui/           shadcn/ui 组件 + 甲骨文主题          ││
│  │  ├── game-core/    游戏核心逻辑 (共享)                   ││
│  │  ├── db/           Prisma schema + 数据库客户端          ││
│  │  ├── types/        共享类型定义 (API/WS)                 ││
│  │  ├── assets/       甲骨文图标与资源索引                  ││
│  │  └── config/       共享配置 (ESLint, TypeScript, etc.)  ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

## 数据流架构

```
┌─────────────────────────────────────────────────────┐
│            浏览器 (Next.js 多应用)                   │
│  - lobby / game / editor                             │
│  - HTML/CSS 渲染 (错位四边形网格)                     │
│  - WebSocket + HTTP                                  │
└───────────────────────┬─────────────────────────────┘
                        │ WebSocket + HTTP
┌───────────────────────▼─────────────────────────────┐
│                  后端服务 (apps/server)              │
│  - WebSocket 实时结算                                │
│  - API / Server Actions (可选)                       │
│  - 游戏引擎 (game-core)                              │
└───────────────────────┬─────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────┐
│                    数据层                             │
│  - PostgreSQL                                        │
│  - Redis (会话缓存，可选)                             │
└─────────────────────────────────────────────────────┘
```

## 为什么选择这个技术栈

### Next.js + Vercel
- 前后端一体化，减少部署复杂度
- Server Actions 简化数据 mutations
- 出色的 DX 和生态系统

### 不使用 Canvas 的原因
- 开发效率更高，可复用现有 UI 组件
- 原生支持 hover、click、tooltip 等交互
- 更好的无障碍支持
- 对于战略模式的简洁界面，HTML/CSS 完全胜任

### Turborepo
- 游戏核心逻辑可在前后端共享
- UI 组件库独立维护
- 类型定义统一管理
